import{_ as s,o as e,c as a,a2 as i}from"./chunks/framework.BWuWLRhz.js";const g=JSON.parse('{"title":"Multi-Agent Collaboration -- PRD","description":"","frontmatter":{},"headers":[],"relativePath":"archive/prds/multi-agent-collaboration.md","filePath":"archive/prds/multi-agent-collaboration.md","lastUpdated":1785228410000}'),t={name:"archive/prds/multi-agent-collaboration.md"};function o(l,n,p,r,c,d){return e(),a("div",null,[...n[0]||(n[0]=[i(`<h1 id="multi-agent-collaboration-prd" tabindex="-1">Multi-Agent Collaboration -- PRD <a class="header-anchor" href="#multi-agent-collaboration-prd" aria-label="Permalink to &quot;Multi-Agent Collaboration -- PRD&quot;">​</a></h1><p><strong>Status:</strong> Draft (discovery interview complete, 2026-07-25) <strong>Author:</strong> Discovery interview (archie-core-abg.1), synthesized from Sam&#39;s answers <strong>Parent epic:</strong> archie-core-abg</p><hr><h2 id="_0-framing" tabindex="-1">0. Framing <a class="header-anchor" href="#_0-framing" aria-label="Permalink to &quot;0. Framing&quot;">​</a></h2><p>Today, &quot;running multiple agents&quot; means running multiple independent <code>archied</code> processes on the same host (<code>carina</code>): <code>archie</code> (bot_user=archie, multiple repositories, deepseek models) and <code>winter</code> (bot_user=winter, a restricted repository set, local llama.cpp models). Confirmed by diffing <code>~/.config/archie/config.toml</code> and <code>~/.config/archie/winter.toml</code> on carina: they differ in <code>bot_user</code>, <code>[[repos]]</code> list, and <code>[models]</code>/<code>[providers]</code> -- not just identity. <code>Config.BotUser</code> is a single global field, so one process = one identity today.</p><p>The goal stated for archie-core overall is to become a fully extensible agentic workhorse -- comparable to and improving on OpenClaw/Hermes. This PRD scopes the &quot;multi-agent&quot; slice of that: what it takes to go from N processes to one daemon that can do what N processes do, plus the surrounding capabilities (chat channels, plugins, memory) needed to reach that bar.</p><h2 id="_1-scope-confirmed-in-interview" tabindex="-1">1. Scope (confirmed in interview) <a class="header-anchor" href="#_1-scope-confirmed-in-interview" aria-label="Permalink to &quot;1. Scope (confirmed in interview)&quot;">​</a></h2><p>All of the following are <strong>in scope</strong> for the epic (not necessarily the first implementation slice -- see section 6):</p><ol><li><strong>Daemon-side concurrency</strong> -- one daemon process running multiple bot identities concurrently, replacing N separate <code>archied</code> processes.</li><li><strong>Cross-repo / cross-config flexibility</strong> -- identities are not statically bound to one repo list at process startup; repo/model/provider configuration varies per identity within a single daemon.</li><li><strong>Agent-to-agent collaboration on a single task</strong> -- one agent&#39;s output feeding another (e.g. plan -&gt; implement -&gt; review handoff), not just N identities running in parallel unaware of each other.</li><li><strong>Conversational access</strong> -- chat channels (Telegram, web UI, more via plugins) where Sam talks to archie directly, outside the Gitea issue-driven flow. Confirmed requirement: <strong>full parity</strong> with the issue-driven flow -- chat is an alternate entry point into the same triage/planner/builder dispatch pipeline, not a separate toy Q&amp;A surface. Chat must be able to: answer questions read-only, spawn/control real work (start an agent, check status, approve/reject a PR, cancel a run), and drive the same pipeline Gitea assignment drives today.</li><li><strong>General extensibility</strong> -- plugins/channels/integrations should be addable, not hardcoded: <ul><li>Additional forges beyond GitHub/Gitea (e.g. GitLab) via the existing <code>forge.Forge</code> interface, as Layer 2 core plugins (see <code>docs/prds/archie-core-v2-architecture.md</code> sec.5).</li><li>MCP server connections (archie as an MCP client, to consume external tools/data sources).</li><li>SSH / remote execution as an agent capability.</li><li>Arbitrary API integrations, callable by agents as tools.</li><li>Additional chat channels beyond Telegram/web UI, as plugins.</li></ul></li><li><strong>Memory systems for multi-session chat</strong> -- conversations with archie need to persist across sessions (distinct from the existing per-task <code>/data/memory.jsonl</code> sketch in the v2 PRD, which is task/project scoped, not conversation scoped).</li><li><strong>Slash-command capabilities</strong> in chat channels (e.g. <code>/status</code>, <code>/spawn</code>, <code>/approve</code>) for direct channel interaction.</li><li><strong>Multi-user support</strong>, as a capability the architecture should not foreclose -- &quot;if I want to extend my agent to multiple people, I should be able to.&quot; Not required to ship in the first slice (see section 5), but the identity/session model must not assume single-operator forever.</li></ol><h2 id="_2-explicitly-out-of-scope-for-now" tabindex="-1">2. Explicitly out of scope (for now) <a class="header-anchor" href="#_2-explicitly-out-of-scope-for-now" aria-label="Permalink to &quot;2. Explicitly out of scope (for now)&quot;">​</a></h2><ul><li><strong>Voice/audio interfaces.</strong> Text-based chat only.</li><li><strong>Building GitHub support</strong> -- it already exists (<code>internal/forge</code> implements GitHub REST polling per <code>ARCHITECTURE.md</code>).</li><li>Anything not listed in section 1 that isn&#39;t a natural extension of the plugin/forge/channel interfaces below.</li></ul><p>Note: multi-user auth and GitLab support are <strong>in scope as capabilities the architecture must support</strong>, but are not committed to the first implementation slice -- see section 5.</p><h2 id="_3-constraints-from-existing-architecture" tabindex="-1">3. Constraints from existing architecture <a class="header-anchor" href="#_3-constraints-from-existing-architecture" aria-label="Permalink to &quot;3. Constraints from existing architecture&quot;">​</a></h2><p>From <code>ARCHITECTURE.md</code> and <code>docs/prds/archie-core-v2-architecture.md</code> (read in full before implementation; summarized here):</p><ul><li><strong><code>Config.BotUser</code> is a single global field</strong> (<code>internal/config</code>) -- the root blocker for daemon-side multi-identity. Becomes a per-identity field in a new config shape (see section 4).</li><li><strong>NATS/JetStream task distribution is implemented</strong>: <code>internal/nats</code> publishes tasks to <code>archie.task.&lt;workflow_type&gt;</code> subjects; agents (in-process, subprocess, or Docker containers per <code>internal/container</code>) consume and report back over <code>archie.agent.&lt;task_id&gt;.{response,system,request}</code>. This subject hierarchy is a natural fit for routing by identity too (e.g. <code>archie.&lt;identity&gt;.task.&lt;type&gt;</code>) but that&#39;s a design decision, not yet made.</li><li><strong>Dispatch is <code>daemon.Run()</code> polling per configured forge/repo list</strong>, serialized per <code>owner/repo</code> by default (<code>AllowConcurrent</code> opts out). Multi-identity daemon means multiple poll loops (or one loop iterating multiple identity configs) feeding the same NATS/container dispatch machinery.</li><li><strong>Layer 2 core plugins (<code>~/.config/archie/plugins/</code>, Yaegi-loaded, <code>Plugin.Register(daemon)</code>) are designed but not wired</strong> -- interface exists (<code>internal/plugin/plugin.go</code>), no loader in <code>cmd/archied/main.go</code>. This is the natural extension point for new forges, MCP client connections, and chat channel plugins. Building the multi-agent epic on top of an unwired plugin layer means <strong>wiring Layer 2 is a prerequisite</strong>, not parallel work.</li><li><strong>No chat/messaging code exists today</strong> -- no Telegram, no web UI beyond the read-only SSE dashboard (<code>internal/webui</code>). This is greenfield.</li><li><strong>The daemon gatekeeps all agent output</strong> -- &quot;no agent output reaches a human without daemon review&quot; (<code>archie-core-v2-architecture.md</code> sec.7). Chat channels must respect this: a chat message that triggers a spawn goes through the same review/labeling machinery PRs and comments do today, not a side channel that bypasses it.</li><li><strong>Environmental constraints over prompt rules</strong> (<code>ARCHITECTURE.md</code> design decision #1) -- chat-triggered actions (spawn, approve, cancel) should be enforced by code (permission checks, rate limits), not by asking the LLM nicely.</li></ul><h2 id="_4-identity-model-design-direction-not-yet-finalized" tabindex="-1">4. Identity model (design direction, not yet finalized) <a class="header-anchor" href="#_4-identity-model-design-direction-not-yet-finalized" aria-label="Permalink to &quot;4. Identity model (design direction, not yet finalized)&quot;">​</a></h2><p>Confirmed requirement: within one daemon, identities differ by bot identity, repo list, <em>and</em> model/provider config (per the archie/winter diff). Proposed shape -- <strong>not yet approved, needs a follow-up design pass before implementation</strong>:</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ~/.config/archie/config.toml (sketch)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">identities</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;archie&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bot_user = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;archie&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">forge = { type = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gitea&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, host = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, token_env = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">models = { triage = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, planner = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, builder = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">repos = [ </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">... </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">identities</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;winter&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bot_user = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;winter&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">forge = { type = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gitea&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, host = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, token_env = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">models = { triage = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, planner = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, builder = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">repos = [ </span><span style="--shiki-light:#B31D28;--shiki-light-font-style:italic;--shiki-dark:#FDAEB7;--shiki-dark-font-style:italic;">... </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span></span></span>
<span class="line"><span>**Design answer (2026-07-25): per-identity goroutine, shared infrastructure.**</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Each identity gets its own goroutine running an independent poll loop with</span></span>
<span class="line"><span>its own forge client, worktree manager, and NATS subject namespace</span></span>
<span class="line"><span>(\`archie.&lt;identity&gt;.task.&lt;type&gt;\`). They share only the infrastructure</span></span>
<span class="line"><span>pieces where isolation adds cost without benefit:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>| Shared (one instance for all identities) | Per-identity (one per goroutine) |</span></span>
<span class="line"><span>|---|---|</span></span>
<span class="line"><span>| SQLite store (\`internal/store\`) | Forge client (different token per identity) |</span></span>
<span class="line"><span>| NATS connection (\`internal/nats\`) | Worktree manager (different \`BotUser\` per identity) |</span></span>
<span class="line"><span>| Container pool (\`internal/container\`) | Config subset: forge, repos, models, providers, dispatch, poll_interval, bot_user, bot_email |</span></span>
<span class="line"><span>| Event bus (\`internal/events\`) | NATS subject namespace: \`archie.&lt;identity&gt;.task.&lt;type&gt;\` |</span></span>
<span class="line"><span>| Web UI (\`internal/webui\`) | Poll loop goroutine |</span></span>
<span class="line"><span>| Plugin registry (\`internal/plugin\`) | |</span></span>
<span class="line"><span>| Gateway router (\`internal/gateway\`) | |</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Decision rationale: per-identity goroutines guarantee failure isolation (one</span></span>
<span class="line"><span>identity&#39;s forge outage cannot block another&#39;s poll tick), each identity</span></span>
<span class="line"><span>gets its own forge rate-limit domain, and the existing \`Daemon\` struct maps</span></span>
<span class="line"><span>cleanly  --  each goroutine holds a thin \`Identity\` value (forge client + config</span></span>
<span class="line"><span>subset + worktree manager + NATS subject prefix) and dispatches tasks with</span></span>
<span class="line"><span>an \`identity_name\` column on the task row. The shared NATS connection</span></span>
<span class="line"><span>multiplexes subjects by identity prefix; the shared store already handles</span></span>
<span class="line"><span>concurrent ClaimNext via SQLite&#39;s serialized writes. NATS subjects carry</span></span>
<span class="line"><span>the identity name so agent containers (which consume from NATS) route</span></span>
<span class="line"><span>responses back to the correct daemon identity.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>This design resolves the open question in section 4 of the original PRD</span></span>
<span class="line"><span>draft (per-identity goroutine vs single loop iterating identities  --  the</span></span>
<span class="line"><span>former, for failure isolation).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## 5. Priority / phasing</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Sam deferred ordering to this PRD (interview answer: &quot;let the interview</span></span>
<span class="line"><span>settle scope, I&#39;ll decide after seeing it written up&quot;). Proposed</span></span>
<span class="line"><span>dependency-ordered phases, for Sam to confirm or reorder:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**Phase A -- Foundation (blocks everything else)**</span></span>
<span class="line"><span>- Wire Layer 2 core plugin loading (\`~/.config/archie/plugins/\`,</span></span>
<span class="line"><span>  \`Plugin.Register(daemon)\`) into \`cmd/archied/main.go\`. Currently designed,</span></span>
<span class="line"><span>  unwired -- both chat channels and new forges depend on this existing.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**Phase B -- Multi-identity daemon core**</span></span>
<span class="line"><span>- Replace single \`Config.BotUser\` with the \`[[identities]]\` shape (section</span></span>
<span class="line"><span>  4, after design confirmation).</span></span>
<span class="line"><span>- Multi-identity poll/dispatch, still issue-driven (Gitea only, no chat yet).</span></span>
<span class="line"><span>- Migrate the two carina processes (archie, winter) onto one daemon as a</span></span>
<span class="line"><span>  real-world validation.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**Phase C -- Chat channel foundation**</span></span>
<span class="line"><span>- Chat channel plugin interface (parallel to \`forge.Forge\`).</span></span>
<span class="line"><span>- Telegram channel plugin: read-only Q&amp;A against task/daemon state first,</span></span>
<span class="line"><span>  then spawn/control actions gated through the same review pipeline as</span></span>
<span class="line"><span>  issue-driven dispatch.</span></span>
<span class="line"><span>- Web UI: extend \`internal/webui\` from read-only SSE dashboard to</span></span>
<span class="line"><span>  bidirectional chat.</span></span>
<span class="line"><span>- Multi-session conversation memory (distinct persistence from per-task</span></span>
<span class="line"><span>  \`/data/memory.jsonl\`).</span></span>
<span class="line"><span>- Slash commands (\`/status\`, \`/spawn\`, \`/approve\`, \`/cancel\`).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**Phase D -- Agent-to-agent collaboration**</span></span>
<span class="line"><span>- Plan -&gt; implement -&gt; review handoff within a single task&#39;s workflow</span></span>
<span class="line"><span>  (builds on existing \`workflow.Stage\` sequencing -- this may already be</span></span>
<span class="line"><span>  close given the \`implement\` workflow&#39;s plan/build stage split; needs</span></span>
<span class="line"><span>  investigation into how much is genuinely new vs. reframing).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**Phase E -- Extended integrations (as needed, each is an independent</span></span>
<span class="line"><span>plugin, no fixed order)**</span></span>
<span class="line"><span>- GitLab forge plugin.</span></span>
<span class="line"><span>- MCP client capability (archie consuming external MCP servers as tools).</span></span>
<span class="line"><span>- SSH / remote execution tool.</span></span>
<span class="line"><span>- Additional chat channels.</span></span>
<span class="line"><span>- Multi-user support (identity/session model extended beyond</span></span>
<span class="line"><span>  single-operator -- needs its own design pass; not blocking Phase A-D).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Sam&#39;s explicit instruction was to identify next requirements **and begin</span></span>
<span class="line"><span>work**, not just deliver a document. Phase A (wiring the Layer 2 plugin</span></span>
<span class="line"><span>loader) is the correct starting point: it&#39;s small, well-specified already in</span></span>
<span class="line"><span>\`docs/prds/archie-core-v2-architecture.md\` sec.5, and unblocks both the chat</span></span>
<span class="line"><span>channel work and new forge plugins that everything else in this PRD depends</span></span>
<span class="line"><span>on.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## 6. Acceptance criteria for this PRD</span></span>
<span class="line"><span></span></span>
<span class="line"><span>- [x] Reflects Sam&#39;s actual interview answers, not assumptions.</span></span>
<span class="line"><span>- [x] Confirms the archie/winter config diff (repo lists differ, not just</span></span>
<span class="line"><span>      identity).</span></span>
<span class="line"><span>- [ ] Broken into scoped, dependency-ordered beads issues under</span></span>
<span class="line"><span>      \`archie-core-abg\`, the way the 5zh epic was. (Next step, this session.)</span></span></code></pre></div>`,19)])])}const u=s(t,[["render",o]]);export{g as __pageData,u as default};
