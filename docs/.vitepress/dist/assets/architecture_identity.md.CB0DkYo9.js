import{_ as t,o as i,c as n,a2 as a}from"./chunks/framework.BWuWLRhz.js";const h=JSON.parse('{"title":"Identity","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/identity.md","filePath":"architecture/identity.md","lastUpdated":null}'),r={name:"architecture/identity.md"};function s(o,e,d,c,l,p){return i(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="identity" tabindex="-1">Identity <a class="header-anchor" href="#identity" aria-label="Permalink to &quot;Identity&quot;">​</a></h1><p><strong>Status:</strong> Approved foundation; lifecycle details are deferred<br><strong>Date:</strong> 2026-07-28<br><strong>Beads issue:</strong> <code>archie-core-5d7</code></p><h2 id="purpose" tabindex="-1">Purpose <a class="header-anchor" href="#purpose" aria-label="Permalink to &quot;Purpose&quot;">​</a></h2><p>Identity answers which persistent actor owns or performed an action. It supplies stable cross-domain attribution and lifecycle semantics.</p><p>Identity kinds include:</p><ul><li>the Archie system identity, such as <code>System</code> or <code>archied</code>;</li><li>agent or bot identities;</li><li>service-account identities;</li><li>future user identities.</li></ul><p>A user is another kind of identity rather than a separate foundation concept.</p><h2 id="ownership" tabindex="-1">Ownership <a class="header-anchor" href="#ownership" aria-label="Permalink to &quot;Ownership&quot;">​</a></h2><p><code>internal/domain/identity</code> owns:</p><ul><li>immutable canonical <code>IdentityID</code> values;</li><li>identity kind;</li><li>lifecycle state;</li><li>ownership and attribution semantics;</li><li>identity-related commands and events.</li></ul><p>Domains may record an identity reference on their own state and events when ownership or attribution is required. They MUST NOT receive a complete per-identity application configuration bundle.</p><p>Persona and communication style are separate from identity.</p><p>Every Agent has its own immutable acting <code>IdentityID</code>. Agent actions use that identity for ownership and attribution in the same way an organisation records which user accessed a resource, changed a permission, or performed another auditable action.</p><p>Agent and Identity remain separate aggregates. Identity supplies the stable actor reference; Agent owns assistant behaviour, memory, specialisation, conversations, and capability context.</p><p>Agent identities MUST NOT be shared between Agents. Automated WorkflowExecutions MAY eventually act through a dedicated service-account identity when a specific unattended automation requires it. Such use is an explicit execution decision and audit principal, not the default for Agents or a substitute for recording the initiating user when one exists.</p><p>A Workflow is owned by an IdentityID and executes as its owner by default. Future delegated execution MAY explicitly assign a different <code>RunAsIdentityID</code>, including a user or service-account identity. Ownership, effective execution identity, initiating identity, and delegation provenance remain distinct audit facts.</p><h2 id="canonical-identity" tabindex="-1">Canonical identity <a class="header-anchor" href="#canonical-identity" aria-label="Permalink to &quot;Canonical identity&quot;">​</a></h2><p>Every identity has an immutable <code>IdentityID</code>. It is the only value used for cross-domain ownership, attribution, routing, persistence, commands, and events.</p><p>Identity kind is explicit:</p><ul><li>system;</li><li>bot;</li><li>service account;</li><li>user.</li></ul><p>Display names are mutable and MUST NOT be used as identifiers. Forge usernames, channel bot names, Git authors, provider account names, and other external identifiers are bindings associated with an identity rather than identity itself.</p><p><code>System</code> is represented by a real identity and <code>IdentityID</code>. The empty string MUST NOT retain special meaning as a legacy system identity.</p><p>Configuration refers to identities by <code>IdentityID</code> and supplies external bindings and capability settings separately. Renaming an identity or changing an external account MUST NOT change ownership or historical attribution.</p><h2 id="persistence" tabindex="-1">Persistence <a class="header-anchor" href="#persistence" aria-label="Permalink to &quot;Persistence&quot;">​</a></h2><p>Identity records are durable domain state. The Identity domain&#39;s repository is the source of truth; infrastructure implements its persistence contract.</p><p>Configuration MAY bootstrap a missing identity through an explicit, audited creation operation and MAY propose runtime capability bindings for an existing <code>IdentityID</code>. Configuration is not the identity registry.</p><p>Omitting an identity from configuration stops assembling the omitted runtime capabilities. It MUST NOT delete the identity record, erase history, or make historical references unresolvable.</p><p>Disabling or retiring an identity requires an explicit Identity-domain command. Retired identities remain resolvable for historical attribution. A failed external binding affects capability availability without destroying or silently replacing the identity.</p><h2 id="lifecycle" tabindex="-1">Lifecycle <a class="header-anchor" href="#lifecycle" aria-label="Permalink to &quot;Lifecycle&quot;">​</a></h2><p>Identity lifecycle is deliberately small:</p><ul><li><code>active</code>: may participate in runtime composition;</li><li><code>suspended</code>: deliberately prevented from initiating new work while ownership and history remain;</li><li><code>retired</code>: terminal, unable to initiate work, and permanently retained for attribution.</li></ul><p>Allowed transitions are:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>create -&gt; active</span></span>
<span class="line"><span>active -&gt; suspended</span></span>
<span class="line"><span>suspended -&gt; active</span></span>
<span class="line"><span>active -&gt; retired</span></span>
<span class="line"><span>suspended -&gt; retired</span></span></code></pre></div><p><code>retired</code> has no outgoing transition.</p><p>Capability health is separate from identity lifecycle. Forge, model, channel, workspace, and other bindings report health independently. An active identity may be partially degraded without becoming suspended or retired. Runtime supervision isolates and restores affected bindings through the safe-change and recovery protocol.</p><h2 id="system-identity" tabindex="-1">System identity <a class="header-anchor" href="#system-identity" aria-label="Permalink to &quot;System identity&quot;">​</a></h2><p>Archie has exactly one built-in system identity. It is created during initial store bootstrap, is strictly always active, and cannot be suspended or retired. Its <code>IdentityID</code> and kind are immutable.</p><p>Startup, migration, supervision, remediation, and rollback actions performed by the application use the system identity. The system identity MUST NOT be delegated to individual identities. Every user, bot, service account, or other actor mutation records the identity that actually performed it.</p><p>System capabilities may independently become unhealthy or unavailable without changing the system identity.</p><h2 id="target-placement-and-contracts" tabindex="-1">Target placement and contracts <a class="header-anchor" href="#target-placement-and-contracts" aria-label="Permalink to &quot;Target placement and contracts&quot;">​</a></h2><p>The Identity domain lives at <code>internal/domain/identity</code>.</p><p>It owns:</p><ul><li><code>IdentityID</code>, identity kind, display name, lifecycle, version, and audit timestamps;</li><li>create, rename, suspend, reactivate, and retire commands;</li><li>corresponding lifecycle events;</li><li>the repository contract required for durable identity records;</li><li>lookup of current and historical identities.</li></ul><p>External account bindings and capability settings remain with the domains that use them and reference <code>IdentityID</code>.</p><h2 id="migration-constraints" tabindex="-1">Migration constraints <a class="header-anchor" href="#migration-constraints" aria-label="Permalink to &quot;Migration constraints&quot;">​</a></h2><p>Migration from the current string model MUST:</p><ul><li>bootstrap the permanent system identity;</li><li>assign stable IDs to configured identities without losing their names;</li><li>replace empty-string legacy ownership with the system identity;</li><li>migrate tasks and sessions to canonical identity references;</li><li>include identity where task uniqueness and transport deduplication require independent ownership;</li><li>route container RPC through the task owner&#39;s capability bindings;</li><li>retain historical resolution after rename, suspension, or retirement;</li><li>remove identity-string equality from authentication or access semantics.</li></ul><p>This completes the foundation required to restructure the current Identity behaviour. Exact storage schemas and Go method signatures belong to implementation design, not this architecture review.</p><h2 id="lifecycle-1" tabindex="-1">Lifecycle <a class="header-anchor" href="#lifecycle-1" aria-label="Permalink to &quot;Lifecycle&quot;">​</a></h2><p>Identity lifecycle is deliberately small:</p><ul><li><code>active</code>: may participate in runtime composition;</li><li><code>suspended</code>: deliberately prevented from initiating new work while ownership and history remain;</li><li><code>retired</code>: terminal, unable to initiate work, and permanently retained for attribution.</li></ul><p>Allowed transitions are:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>create -&gt; active</span></span>
<span class="line"><span>active -&gt; suspended</span></span>
<span class="line"><span>suspended -&gt; active</span></span>
<span class="line"><span>active -&gt; retired</span></span>
<span class="line"><span>suspended -&gt; retired</span></span></code></pre></div><p><code>retired</code> has no outgoing transition.</p><p>Capability health is separate from identity lifecycle. Forge, model, channel, workspace, and other bindings report health independently. An active identity may be partially degraded without becoming suspended or retired. Runtime supervision isolates and restores affected bindings through the safe-change and recovery protocol.</p><h2 id="explicit-non-responsibilities" tabindex="-1">Explicit non-responsibilities <a class="header-anchor" href="#explicit-non-responsibilities" aria-label="Permalink to &quot;Explicit non-responsibilities&quot;">​</a></h2><p>Identity is not authentication and is not access control.</p><p>The current architecture does not require authentication or authorization. Those capabilities MAY be introduced later if deployment or product needs justify them. They MUST remain separate from the identity model.</p><p>An identity reference therefore proves attribution within Archie&#39;s trusted system model; it does not by itself prove credentials or permission.</p><h2 id="per-identity-configuration" tabindex="-1">Per-identity configuration <a class="header-anchor" href="#per-identity-configuration" aria-label="Permalink to &quot;Per-identity configuration&quot;">​</a></h2><p>Values selected for an identity remain external configuration values. At composition time they are split into the runtime settings owned by the affected domains and correlated by identity reference.</p><p>The existing <code>IdentityConfig</code> aggregate MUST be dissolved. Forge accounts, repositories, models, providers, workspace behaviour, polling, budgets, notifications, and transport namespaces retain their respective owners.</p><h2 id="audit" tabindex="-1">Audit <a class="header-anchor" href="#audit" aria-label="Permalink to &quot;Audit&quot;">​</a></h2><p>Configuration changes and other state changes are actions attributed to an identity. System-initiated changes use the system identity. Their audit records MUST preserve the acting identity, the target, and the resulting change.</p><p>The command ownership and persistence mechanism for configuration changes is a separate configuration-management decision.</p><h2 id="current-implementation" tabindex="-1">Current implementation <a class="header-anchor" href="#current-implementation" aria-label="Permalink to &quot;Current implementation&quot;">​</a></h2><p>Identity is not currently implemented as a domain. It is a collection of string fields and process-local associations:</p><ul><li><code>config.IdentityConfig.Name</code> is the daemon routing key.</li><li><code>config.IdentityConfig.BotUser</code> is the external forge and Git author name.</li><li><code>daemon.IdentityRunner</code> bundles the configured name with a forge client, worktree manager, repository list, configuration subset, logger, and polling goroutine.</li><li><code>store.Task.Identity</code> persists the configured name on tasks.</li><li>NATS task messages copy the same configured name.</li><li>gateway routing accepts an identity name and uses it to select a repository profile.</li><li>gateway sessions persist <code>BotUser</code>, not the configured identity name.</li></ul><p>There is no identity registry, identity record, identity lifecycle state, identity persistence, or identity command/event model. Removing an identity from configuration makes existing task ownership unresolvable.</p><p>An empty identity string selects the legacy single-identity path. This gives the empty string behavioural meaning rather than representing a real system identity.</p><h2 id="current-enforcement-and-isolation" tabindex="-1">Current enforcement and isolation <a class="header-anchor" href="#current-enforcement-and-isolation" aria-label="Permalink to &quot;Current enforcement and isolation&quot;">​</a></h2><p>The current implementation uses identity equality as an authorization check for chat task approval and cancellation. This conflicts with the approved rule that identity is attribution rather than authentication or access control and must be reassigned to an appropriate task-ownership or future access policy.</p><p>Focused tests verify that:</p><ul><li>a task resolves the forge client and repository list associated with its configured identity name;</li><li>cross-identity repository profiles do not leak;</li><li>chat task creation propagates the selected identity;</li><li>task and NellDB persistence retain the identity string;</li><li>chat task control rejects a different identity string.</li></ul><h2 id="current-hazards" tabindex="-1">Current hazards <a class="header-anchor" href="#current-hazards" aria-label="Permalink to &quot;Current hazards&quot;">​</a></h2><ul><li>SQLite task uniqueness is <code>(owner, repo, issue_number)</code> and excludes identity. Two identities discovering the same forge issue cannot own distinct task records.</li><li>NATS deduplication uses <code>owner/repo/issue</code> and excludes identity, so discovery by a second identity can be discarded as a duplicate.</li><li>Container-mode forge and worktree RPC servers are wired to the root clients, not the task owner&#39;s clients. The source documents this as an unresolved identity leak.</li><li>The shared task queue claims the oldest task without identity partitioning and relies on later string lookup to recover the correct runner.</li><li>Sessions use forge bot usernames where tasks use configured identity names, so there is no canonical cross-domain identity reference.</li><li>Identity lifecycle is process lifetime only; there is no disabled, degraded, retired, or unavailable state and no audit history.</li></ul><p>The focused identity tests pass. Broader daemon and NATS suites could not open embedded listener ports in the current sandbox; their failures occurred before identity behaviour executed.</p>`,77)])])}const y=t(r,[["render",s]]);export{h as __pageData,y as default};
