import{_ as o,o as t,c as a,a2 as s}from"./chunks/framework.BWuWLRhz.js";const p=JSON.parse('{"title":"Archie Design Decisions","description":"","frontmatter":{},"headers":[],"relativePath":"archive/decisions.md","filePath":"archive/decisions.md","lastUpdated":1785228410000}'),r={name:"archive/decisions.md"};function n(i,e,d,c,l,g){return t(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="archie-design-decisions" tabindex="-1">Archie Design Decisions <a class="header-anchor" href="#archie-design-decisions" aria-label="Permalink to &quot;Archie Design Decisions&quot;">​</a></h1><p>Record of architectural choices, reversals, and lessons learned during development. Each entry has a date, a decision, the reasoning, and the context that led to it. This document is the first thing to read before changing anything that contradicts a prior decision.</p><hr><h2 id="_2026-07-23-forge-agnostic-issue-creation-via-createissue" tabindex="-1">2026-07-23 -- Forge-agnostic issue creation via CreateIssue <a class="header-anchor" href="#_2026-07-23-forge-agnostic-issue-creation-via-createissue" aria-label="Permalink to &quot;2026-07-23  --  Forge-agnostic issue creation via CreateIssue&quot;">​</a></h2><p><strong>Decision</strong>: Add <code>CreateIssue(ctx, owner, repo, title, body, labels) (int, error)</code> to the <code>forge.Forge</code> interface. Every forge backend (Gitea, GitHub, future beads) implements it.</p><p><strong>Reasoning</strong>: Agents discover out-of-scope findings during planning and building. These should be filed as tickets immediately rather than lost. Making <code>CreateIssue</code> part of the Forge interface means the agent calls one method regardless of the underlying ticketing system.</p><p><strong>Future</strong>: A <code>beads</code> forge backend will implement <code>CreateIssue</code> by calling <code>bd create</code>. This makes beads the internal/default issue tracker while Gitea/GitHub are external forges. The agent doesn&#39;t know which it&#39;s talking to -- the daemon resolves the forge from repo config.</p><hr><h2 id="_2026-07-23-dolt-and-beads-for-agent-native-issue-tracking" tabindex="-1">2026-07-23 -- Dolt and Beads for agent-native issue tracking <a class="header-anchor" href="#_2026-07-23-dolt-and-beads-for-agent-native-issue-tracking" aria-label="Permalink to &quot;2026-07-23  --  Dolt and Beads for agent-native issue tracking&quot;">​</a></h2><p><strong>Decision</strong>: Install dolt (version-controlled SQL) and beads (<code>bd</code>, a graph-based issue tracker for AI agents) as the foundation for multi-repo scaling and multi-agent coordination.</p><p><strong>Reasoning</strong>: Beads provides persistent memory across sessions, first- class dependency tracking, JSON output for programmatic use, and hash- based IDs that prevent merge conflicts in multi-agent workflows. It&#39;s built on Dolt which gives cell-level merge, native branching, and remote sync -- the same primitives git gives code but applied to issues.</p><p><strong>Context</strong>: Gas Town (<code>~/gt/</code>) is cloned for reference -- it implements a multi-agent workspace on top of beads. The archie-core scaling strategy will follow a similar model: a coordinator daemon with per-repo agents, all sharing a beads-backed task graph.</p><p><strong>Current state</strong>: dolt 2.2.2, beads 1.1.0 installed. Beads initialized in archie-core (<code>.beads/</code>). Six issues seeded for the scaling roadmap.</p><hr><h2 id="_2026-07-23-token-budgets-must-be-generous-not-predictive" tabindex="-1">2026-07-23 -- Token budgets must be generous, not predictive <a class="header-anchor" href="#_2026-07-23-token-budgets-must-be-generous-not-predictive" aria-label="Permalink to &quot;2026-07-23  --  Token budgets must be generous, not predictive&quot;">​</a></h2><p><strong>Decision</strong>: Set <code>max_tokens</code> to 100,000,000 (100M). Do not attempt to predict what a task needs without data. Lower limits later based on actual per-workflow/per-repo metrics.</p><p><strong>Reasoning</strong>: An agent killed mid-task at 1M tokens wastes every token it already consumed. There is no compaction or checkpointing yet, so a killed agent is a total loss. Hermes regularly uses 160+ steps and hundreds of thousands of tokens per request. Large repositories need proportionally more.</p><p><strong>Context</strong>: The planner burned 357K tokens just to discover a fix already existed. The builder hit 1M at step 25 of 90. Neither task was unreasonable -- they were just exploring a large codebase.</p><p><strong>Future</strong>: Track actual token consumption per workflow/stage/repo. Use that data to set informed limits. Build compaction or checkpointing so a killed agent can resume rather than restart.</p><hr><h2 id="_2026-07-23-baseline-fix-auto-repair-pre-existing-gate-failures" tabindex="-1">2026-07-23 -- baseline-fix: auto-repair pre-existing gate failures <a class="header-anchor" href="#_2026-07-23-baseline-fix-auto-repair-pre-existing-gate-failures" aria-label="Permalink to &quot;2026-07-23  --  baseline-fix: auto-repair pre-existing gate failures&quot;">​</a></h2><p><strong>Decision</strong>: When <code>StageBaselineGate</code> detects a red gate (gofumpt, go vet, etc. failing on clean main), launch the builder to fix the failures via TDD rather than parking. Only park if the builder cannot fix them.</p><p><strong>Reasoning</strong>: A red baseline means the repo was broken before archie touched it. Parking creates an infinite loop -- the next retry hits the same failure. The agent should fix pre-existing issues, not fail on them.</p><p><strong>Implementation</strong>: <code>StageBaselineGate</code> now calls <code>tc.Agent.Run()</code> with a TDD mission when the gate fails. Uses the same builder agent. Note: this required wiring <code>tc.Agent</code> (the runner) through the stage context.</p><hr><h2 id="_2026-07-23-no-hardcoded-maxsteps-in-workflow-stages" tabindex="-1">2026-07-23 -- No hardcoded MaxSteps in workflow stages <a class="header-anchor" href="#_2026-07-23-no-hardcoded-maxsteps-in-workflow-stages" aria-label="Permalink to &quot;2026-07-23  --  No hardcoded MaxSteps in workflow stages&quot;">​</a></h2><p><strong>Decision</strong>: Remove all hardcoded <code>MaxSteps</code> values from workflow stages. Every stage inherits from <code>[budgets].max_steps</code> in the config.</p><p><strong>Reasoning</strong>: Hardcoded values (15 for plan, 15 for TDD analyse, 15 for feasibility assess, 20 for PRD) were arbitrary and broke on large repos. The config should be the single source of truth.</p><p><strong>Files affected</strong>: <code>implement.go</code>, <code>tdd.go</code>, <code>feasibility.go</code>.</p><hr><h2 id="_2026-07-23-no-op-detection-close-issues-when-build-has-0-changes" tabindex="-1">2026-07-23 -- No-op detection: close issues when build has 0 changes <a class="header-anchor" href="#_2026-07-23-no-op-detection-close-issues-when-build-has-0-changes" aria-label="Permalink to &quot;2026-07-23  --  No-op detection: close issues when build has 0 changes&quot;">​</a></h2><p><strong>Decision</strong>: When the builder completes with <code>StatusPassed</code> and 0 file changes, close the issue with a comment rather than parking on &quot;worktree has no changes to commit&quot;.</p><p><strong>Reasoning</strong>: A builder that passes with 0 changes means the fix already exists or the issue is a no-op. The old behavior parked in an infinite loop (commit-push fails because nothing changed, task re-queues, builder runs again, same result).</p><p><strong>Implementation</strong>: <code>TaskContext.BuildNoChanges</code> is set in the build stage&#39;s <code>OnResult</code> when <code>Status == Passed &amp;&amp; len(Changes) == 0</code>. <code>StageCommitPush</code> checks this flag and calls <code>Forge.CloseIssue()</code> + sets <code>Outcome = Merged</code> to stop the workflow.</p><p><strong>Test</strong>: <code>TestStageCommitPushClosesIssueWhenBuildNoChanges</code> in <code>implement_test.go</code>.</p><hr><h2 id="_2026-07-23-assignee-trigger-over-label-trigger" tabindex="-1">2026-07-23 -- assignee trigger over label trigger <a class="header-anchor" href="#_2026-07-23-assignee-trigger-over-label-trigger" aria-label="Permalink to &quot;2026-07-23  --  assignee trigger over label trigger&quot;">​</a></h2><p><strong>Decision</strong>: Default to <code>trigger = &quot;assignee&quot;</code> instead of <code>trigger = &quot;label&quot;</code>. The daemon only claims issues explicitly assigned to the bot user.</p><p><strong>Reasoning</strong>: Codex created 80+ issues with the <code>archie</code> label, and the daemon auto-claimed every one. Label-based triggering is too promiscuous for repos shared between humans and agents.</p><hr><h2 id="_2026-07-23-deepseek-v4-pro-as-primary-model" tabindex="-1">2026-07-23 -- DeepSeek v4 Pro as primary model <a class="header-anchor" href="#_2026-07-23-deepseek-v4-pro-as-primary-model" aria-label="Permalink to &quot;2026-07-23  --  DeepSeek v4 Pro as primary model&quot;">​</a></h2><p><strong>Decision</strong>: Use <code>deepseek/deepseek-v4-pro</code> as the planner and builder model. Qwythos-9B (local llama.cpp) is the fallback for offline/cheap tasks.</p><p><strong>Reasoning</strong>: Qwythos couldn&#39;t complete a representative large-repository plan stage before timing out. DeepSeek completed it in 21 iterations. Large repositories need a larger model.</p><p><strong>Provider setup</strong>: <code>class = &quot;deepseek&quot;</code>, API key via <code>DEEPSEEK_API_KEY</code> env var from Bitwarden Secrets Manager (bws).</p><hr><h2 id="_2026-07-22-v1-0-0-tagged" tabindex="-1">2026-07-22 -- v1.0.0 tagged <a class="header-anchor" href="#_2026-07-22-v1-0-0-tagged" aria-label="Permalink to &quot;2026-07-22  --  v1.0.0 tagged&quot;">​</a></h2><p><strong>Decision</strong>: Tagged <code>v1.0.0</code> at commit incorporating the daemon/agent split, NATS JetStream, Docker sandbox, Yaegi plugins, agentskills.io skills, and Gitea forge support.</p><hr><h2 id="_2026-07-22-docker-compose-stack-with-nats-containers" tabindex="-1">2026-07-22 -- Docker Compose stack with NATS + containers <a class="header-anchor" href="#_2026-07-22-docker-compose-stack-with-nats-containers" aria-label="Permalink to &quot;2026-07-22  --  Docker Compose stack with NATS + containers&quot;">​</a></h2><p><strong>Decision</strong>: The production deployment uses <code>docker-compose.yml</code> with NATS, archied, and archie-agent containers. Config at <code>config.docker.toml</code>.</p><p><strong>Key fixes</strong>:</p><ul><li>NATS healthcheck uses <code>wget localhost:8222/healthz</code> (not <code>nats-server check</code>)</li><li><code>host.docker.internal:host-gateway</code> extra_hosts for Linux</li><li>Config mount path: <code>config.docker.toml</code> → <code>/etc/archie/config.toml</code></li><li>No stale <code>image:</code> tag on archied service (forces local build)</li></ul><hr><h2 id="_2026-07-22-worktree-prepare-before-container-acquire" tabindex="-1">2026-07-22 -- Worktree Prepare before Container Acquire <a class="header-anchor" href="#_2026-07-22-worktree-prepare-before-container-acquire" aria-label="Permalink to &quot;2026-07-22  --  Worktree Prepare before Container Acquire&quot;">​</a></h2><p><strong>Decision</strong>: Clone the worktree (<code>Trees.Prepare()</code>) before acquiring a Docker container. Made <code>Prepare()</code> idempotent -- skips if <code>.git</code> already exists.</p><p><strong>Reasoning</strong>: Docker bind-mounts fail with &quot;bind source path does not exist&quot; if the worktree hasn&#39;t been cloned yet. The old code acquired the container first, then the prepare stage cloned into a path that didn&#39;t exist on the host.</p><hr><h2 id="_2026-07-22-gofumpt-and-go-tools-must-be-on-path" tabindex="-1">2026-07-22 -- Gofumpt and Go tools must be on PATH <a class="header-anchor" href="#_2026-07-22-gofumpt-and-go-tools-must-be-on-path" aria-label="Permalink to &quot;2026-07-22  --  Gofumpt and Go tools must be on PATH&quot;">​</a></h2><p><strong>Decision</strong>: The daemon&#39;s <code>exec.Command</code> for gate checks inherits the process PATH. <code>~/go/bin</code> (where <code>gofumpt</code>, <code>golangci-lint</code>, etc. live) must be in the PATH before starting the daemon.</p><p><strong>Symptom</strong>: <code>baseline red -- gofumpt -w . fails</code> but manual run passes. Root cause: <code>which gofumpt</code> returns nothing because <code>~/go/bin</code> isn&#39;t on PATH in Hermes sessions.</p>',60)])])}const u=o(r,[["render",n]]);export{p as __pageData,u as default};
