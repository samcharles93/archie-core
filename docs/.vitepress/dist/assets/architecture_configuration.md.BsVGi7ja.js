import{_ as t,o,c as i,a2 as a}from"./chunks/framework.BWuWLRhz.js";const h=JSON.parse('{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/configuration.md","filePath":"architecture/configuration.md","lastUpdated":null}'),n={name:"architecture/configuration.md"};function d(r,e,c,s,l,u){return o(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h1><p><strong>Status:</strong> Approved; existing-type assignment is ongoing<br><strong>Date:</strong> 2026-07-28<br><strong>Beads issue:</strong> <code>archie-core-5d7</code></p><h2 id="ownership" tabindex="-1">Ownership <a class="header-anchor" href="#ownership" aria-label="Permalink to &quot;Ownership&quot;">​</a></h2><p>Configuration has two separate responsibilities:</p><ol><li><code>internal/infrastructure/configuration</code> loads files, environment variables, secret references, overlays, and compatibility representations.</li><li>Each domain and plugin owns the typed runtime settings required for its behaviour.</li></ol><p><code>internal/app</code> translates external input into those settings, validates cross-capability combinations, and supplies each component only its own settings.</p><p>The following are prohibited:</p><ul><li>passing complete application configuration into a domain;</li><li>making a domain depend on TOML, YAML, environment-variable names, secret references, or a configuration library;</li><li>defining a central runtime configuration type imported across the system;</li><li>placing plugin options in an unrelated global configuration model.</li></ul><h2 id="private-input-document" tabindex="-1">Private input document <a class="header-anchor" href="#private-input-document" aria-label="Permalink to &quot;Private input document&quot;">​</a></h2><p><code>internal/infrastructure/configuration</code> MAY use a complete decoded document while discovering files, decoding sections, and applying overlays. It is an input-boundary DTO only.</p><p>The document:</p><ul><li>remains private to the configuration infrastructure;</li><li>is composed from separately defined feature sections;</li><li>is immediately translated by application composition;</li><li>is never returned as the application&#39;s runtime model;</li><li>is never passed through runtime components.</li></ul><p>External DTOs and domain runtime settings are separate types.</p><p>Plugins supply external configuration definitions through a narrow extension contract. The core loader MUST NOT gain a field or recognized filename for every plugin.</p><p>Invalid configuration input MUST NOT replace active valid settings or terminate unaffected capabilities. Configuration changes use the candidate validation, promotion, audit, and recovery requirements in <a href="./safe-change-and-recovery">safe-change-and-recovery.md</a>.</p><h2 id="dissolution-of-internal-config" tabindex="-1">Dissolution of <code>internal/config</code> <a class="header-anchor" href="#dissolution-of-internal-config" aria-label="Permalink to &quot;Dissolution of `internal/config`&quot;">​</a></h2><p><code>internal/config</code> has no target location. It MUST be dissolved and MUST NOT survive as a shared package, configuration domain, or renamed global model.</p><p>Its loading behaviour moves to <code>internal/infrastructure/configuration</code>. Existing types and methods are reassigned by behaviour. Repository execution policy, dispatch policy, retry policy, ecosystem defaults, tool policy, memory settings, indexing settings, provider settings, and channel settings move to their respective owners.</p><h2 id="repository-settings" tabindex="-1">Repository settings <a class="header-anchor" href="#repository-settings" aria-label="Permalink to &quot;Repository settings&quot;">​</a></h2><p>The existing <code>config.Repo</code> MUST NOT move intact into a repository domain or shared settings package. A managed repository is an application association:</p><ul><li>work intake owns which repository references an identity monitors;</li><li>task execution owns gates, preflight, test protection, ecosystem behaviour, and change limits;</li><li>task lifecycle owns retry behaviour;</li><li>scheduling owns concurrency policy;</li><li>workspace management owns base branch and persistent-storage requirements.</li></ul><p>Application composition correlates these settings by repository reference. No domain receives a universal repository profile.</p><h2 id="documentation" tabindex="-1">Documentation <a class="header-anchor" href="#documentation" aria-label="Permalink to &quot;Documentation&quot;">​</a></h2><p>Generated configuration reference describes external sections and their owning capabilities. It MUST NOT establish a global Go configuration structure as the public contract.</p><h2 id="value-classification-rule" tabindex="-1">Value classification rule <a class="header-anchor" href="#value-classification-rule" aria-label="Permalink to &quot;Value classification rule&quot;">​</a></h2><p>Every value that controls behaviour MUST have an explicit owner and MUST be classified as exactly one of:</p><ol><li><strong>Invariant</strong> — changing it changes the meaning of the program, a wire protocol, or a domain concept. It may be a Go constant.</li><li><strong>Runtime setting</strong> — an operator, identity, domain, plugin, or deployment may legitimately choose a different value. It is typed configuration owned by the affected capability.</li><li><strong>Policy value</strong> — it expresses what is allowed, limited, routed, retried, protected, or gated. Its typed definition belongs to the owning domain and it is evaluated through the policy system.</li><li><strong>Derived value</strong> — it is computed from other settings and is not independently mutable.</li><li><strong>Runtime state</strong> — it changes as the application operates, such as the active model for a conversation. It is changed by a domain command and is not configuration merely because it can be changed.</li></ol><p>An unclassified behavioural literal is a defect.</p><p><code>const</code> MUST NOT be used merely because a value currently has one default. Constants are reserved for actual invariants: enum discriminants, protocol versions, fixed compatibility tokens, and values whose alteration would change the program&#39;s semantics rather than configure an installation.</p><p>In Go, a constant is a compile-time value and does not represent mutable runtime storage. It generally has no address that can be observed or changed. Architecturally, a constant therefore asserts that the value cannot change for the lifetime of the application. Any value that may legitimately change while the application is running, between application instances, or between deployments is not a constant.</p><p>A runtime setting may have a default, but the default is not its only access path. The owner exposes a typed settings structure, validation, and a <code>DefaultSettings</code> constructor or equivalent. Private constants used to implement defaults do not turn those defaults into invariants and MUST NOT be consumed directly by runtime code.</p><p>Each runtime setting and policy value MUST declare:</p><ul><li>its owner;</li><li>type, units, validation constraints, and safe bounds;</li><li>default and derivation, if any;</li><li>whether it is startup-only, live-reloadable, or restart-required;</li><li>whether it may differ by installation, identity, repository, workflow, channel, or plugin instance;</li><li>whether it is secret, sensitive, or safe to report;</li><li>its current source, effective value, version, and last-known-good version;</li><li>the command and identity required to change it.</li></ul><h2 id="current-configuration-migration-ledger" tabindex="-1">Current configuration migration ledger <a class="header-anchor" href="#current-configuration-migration-ledger" aria-label="Permalink to &quot;Current configuration migration ledger&quot;">​</a></h2><p>This ledger records the current meanings hidden inside <code>internal/config</code>. It is the source inventory for dissolving that package; rows are split where the existing structure combines different owners.</p><table tabindex="0"><thead><tr><th>Current values</th><th>Current location</th><th>Target owner and requirement</th></tr></thead><tbody><tr><td><code>WorkDir</code>, <code>DBPath</code></td><td>root <code>Config</code></td><td>Workspace/storage infrastructure settings. Defaults derive from the platform data directory.</td></tr><tr><td><code>SkillsDir</code>, <code>PluginDir</code></td><td>root <code>Config</code></td><td>Skill and plugin infrastructure settings, independently typed.</td></tr><tr><td><code>PollInterval</code></td><td>root <code>Config</code></td><td>Work-intake scheduling setting; not a daemon-wide constant.</td></tr><tr><td><code>Label</code>, <code>BotUser</code>, <code>BotEmail</code></td><td>root <code>Config</code></td><td>Identity and dispatch associations. <code>BotEmail</code> may be derived from the selected forge account but remains overrideable.</td></tr><tr><td><code>MaxRetries</code></td><td>root <code>Config</code>, <code>Repo</code></td><td>Retry policy owned by the operation being retried. The duplicate global/repository representation must disappear.</td></tr><tr><td><code>DiffCapLines</code></td><td>root <code>Config</code></td><td>Agent-change policy value.</td></tr><tr><td><code>MaxSteps</code>, <code>MaxTokens</code>, <code>WallClock</code>, <code>GateMaxFailures</code></td><td><code>Budgets</code></td><td>Workflow-execution policy, with units and zero/unlimited semantics made explicit.</td></tr><tr><td><code>Class</code>, <code>APIKeyEnv</code>, <code>BaseURL</code></td><td><code>Provider</code></td><td>Provider instance settings. Secret lookup becomes a <code>SecretRef</code>, not an environment variable name leaking into the provider domain.</td></tr><tr><td><code>Mode</code>, <code>Command</code>, <code>Env</code></td><td><code>Agent</code></td><td>Agent-execution infrastructure settings. Arbitrary environment injection is explicitly trust-sensitive.</td></tr><tr><td><code>Type</code>, <code>Host</code>, <code>Token</code>/legacy <code>TokenEnv</code></td><td><code>Forge</code></td><td>Forge adapter settings. Legacy input is accepted only at the loader boundary.</td></tr><tr><td><code>Trigger</code>, <code>AckReaction</code>, label names</td><td><code>Dispatch</code></td><td>Work-intake and forge presentation policy. Label vocabulary is configurable per forge/identity association.</td></tr><tr><td><code>Provider</code>, <code>ProviderConfig</code>, <code>SessionTTL</code></td><td><code>MemoryConfig</code></td><td>Memory capability settings. Provider-specific data remains owned and validated by that provider.</td></tr><tr><td><code>Name</code>, <code>Transport</code>, <code>Command</code>, <code>Args</code>, <code>WorkDir</code>, <code>URL</code></td><td><code>MCPServer</code></td><td>One typed MCP server instance specification. Transport-specific variants must reject fields belonging to other variants.</td></tr><tr><td><code>MaxResultChars</code>, <code>ParallelExecution</code></td><td><code>ToolPolicy</code></td><td>Tool-execution policy values, not generic daemon configuration.</td></tr><tr><td><code>URL</code>, <code>TokenEnv</code></td><td><code>NATSConfig</code></td><td>NATS transport settings with a secret reference replacing the environment-name field.</td></tr><tr><td><code>Enabled</code>, <code>Image</code>, <code>MaxConcurrency</code>, <code>MaxUptime</code>, <code>VolumeTTL</code>, <code>PullPolicy</code>, <code>Network</code></td><td><code>ContainerConfig</code></td><td>Container-execution infrastructure settings and resource policy.</td></tr><tr><td>model mappings, email, webhook, Telegram</td><td><code>ChatConfig</code></td><td>Separate channel instance settings plus channel-routing associations; there is no chat-wide settings owner.</td></tr><tr><td><code>AllowedUserIDs</code>, <code>TokenEnv</code>, update commands</td><td>Telegram config</td><td>Telegram adapter settings and access policy. Secret and policy fields are distinct.</td></tr><tr><td>listen/relay addresses</td><td>email config</td><td>Email adapter instance settings.</td></tr><tr><td>webhook target</td><td><code>Notify</code></td><td>Notification adapter instance settings.</td></tr><tr><td>web listen address</td><td><code>Web</code></td><td>Web transport settings.</td></tr><tr><td>index directory and database path</td><td><code>Indexing</code></td><td>Index infrastructure settings, derived from storage settings unless explicitly overridden.</td></tr><tr><td>forge, dispatch, models, providers, agent, containers, memory, tools, indexing and repositories</td><td><code>IdentityConfig</code></td><td>Existing composite duplicates unrelated settings. Replace it with associations from an <code>IdentityID</code> to independently owned configured instances.</td></tr><tr><td>owner, name, base, gate, protection, ecosystem, preflight, storage, retry and concurrency</td><td><code>Repo</code></td><td>Split across work intake, workspace, workflow execution, policy, gate, and scheduling as described in Repository settings.</td></tr><tr><td><code>TaskConfig</code>, <code>TaskForge</code></td><td>worker snapshot</td><td>Replace with the minimum immutable, versioned WorkflowExecution input assembled by the application. It is not a second configuration model.</td></tr><tr><td><code>Extra</code> and arbitrary maps</td><td>root/provider config</td><td>Allowed only at an extension boundary whose owning plugin/provider supplies schema and validation. Core configuration never consumes an untyped catch-all.</td></tr></tbody></table><h2 id="behavioural-literals-and-incorrectly-fixed-values" tabindex="-1">Behavioural literals and incorrectly fixed values <a class="header-anchor" href="#behavioural-literals-and-incorrectly-fixed-values" aria-label="Permalink to &quot;Behavioural literals and incorrectly fixed values&quot;">​</a></h2><p>The following current values affect useful behaviour but are not represented as owned typed settings or policy. They MUST be classified and migrated before the old configuration package is removed. The listed target is the architectural decision; exact setting names are implementation details.</p><table tabindex="0"><thead><tr><th>Capability</th><th>Values fixed in current code</th><th>Required classification</th></tr></thead><tbody><tr><td>Chat agent</td><td>maximum steps <code>8</code> in <code>cmd/archied</code></td><td>Workflow/chat execution policy.</td></tr><tr><td>Context compression</td><td>enabled, threshold <code>0.5</code>, context size <code>128000</code>, protect first <code>3</code> and last <code>20</code>, compression marker, token estimate of four characters per token</td><td>Model/context runtime settings. The marker is presentation; the estimate is provider/model strategy, not a universal truth.</td></tr><tr><td>Tool guardrails</td><td>escalation thresholds <code>3</code>, <code>5</code>, <code>10</code>, <code>2</code></td><td>Tool-use policy with named meanings and bounded defaults.</td></tr><tr><td>Tool output</td><td>turn budget <code>200000</code> characters and preview <code>500</code> characters</td><td>Tool-execution resource and presentation settings.</td></tr><tr><td>Pairing/access</td><td>code TTL <code>1h</code>, maximum pending <code>3</code>, rate window <code>10m</code>, failures <code>5</code>, lockout <code>1h</code>, maximum lockout <code>24h</code></td><td>Access policy. Code length/alphabet and salt length require an explicit security-constraint decision; they are not automatically valid constants.</td></tr><tr><td>NATS daemon consumer</td><td>stream <code>ARCHIE_TASKS</code>, consumer <code>archie-daemon</code>, deduplication <code>2m</code>, poll <code>2s</code>, deliveries <code>3</code>, acknowledgement wait <code>5m</code>, inactive threshold <code>24h</code>, subjects</td><td>NATS topology settings plus delivery policy. Subject grammar or protocol headers may be invariants only if declared as versioned wire contracts.</td></tr><tr><td>NATS worker consumer</td><td>stream <code>ARCHIE_TASKS</code>, consumer <code>archie-agent</code>, deduplication <code>2m</code>, poll <code>5s</code>, acknowledgement wait <code>30m</code>, deliveries <code>3</code>, inactive threshold <code>24h</code>, queue group and retry delay <code>1s</code></td><td>Worker transport settings and delivery/retry policy. Shared stream defaults must have one owner rather than two copies.</td></tr><tr><td>Worker readiness</td><td>readiness timeout <code>20s</code>, retry delay <code>250ms</code></td><td>Worker lifecycle and retry policy.</td></tr><tr><td>Execution/RPC</td><td>agent execution <code>30m</code>, task-run RPC <code>60s</code>, worktree RPC <code>15m</code></td><td>Per-operation timeout policy.</td></tr><tr><td>Indexing</td><td>build timeout <code>10m</code>, metadata timeout <code>5s</code></td><td>Index operation policy.</td></tr><tr><td>Plugin/tool cleanup</td><td>plugin rollback <code>10s</code>, tool-provider cleanup <code>10s</code>, MCP-provider cleanup <code>10s</code></td><td>Lifecycle shutdown/rollback policy owned by each engine, with a shared policy vocabulary where appropriate.</td></tr><tr><td>Memory</td><td>sync buffer <code>100</code>, prefetch <code>8s</code>, shutdown <code>10s</code>, single external-provider limit</td><td>Memory resource/lifecycle settings. The provider-count restriction is a product rule only if deliberately retained.</td></tr><tr><td>Built-in memory store</td><td>file limit <code>100KiB</code>, section-name length/character rules, query/match truncation limits, built-in provider/tool names</td><td>Split into memory resource policy, input policy, presentation settings, and genuine registered identifiers.</td></tr><tr><td>Memory safety scanner</td><td>compiled prompt-injection and sensitive-data patterns with fixed warn/block consequences</td><td>Versioned memory-domain policy definitions. Patterns and consequences must be inspectable and replaceable without editing scanner code.</td></tr><tr><td>MCP stdio</td><td>initial backoff <code>500ms</code>, maximum backoff <code>30s</code>, unlimited default retries, send and shutdown <code>5s</code></td><td>MCP transport retry, request, and lifecycle policy. Unlimited retries must be explicit rather than a zero-value accident.</td></tr><tr><td>MCP SSE/HTTP</td><td>reconnect <code>1s</code> to <code>30s</code>, POST/request timeout <code>30s</code></td><td>MCP transport settings and retry policy.</td></tr><tr><td>Message framing</td><td>MCP frame <code>4MiB</code>, worker protocol frame <code>8MiB</code>, diagnostic <code>64KiB</code>, media/base64 <code>64MiB</code></td><td>Resource/security policy unless a published wire protocol mandates the limit. Limits must be configurable only within safe bounds.</td></tr><tr><td>Webhooks and web UI</td><td>webhook bind <code>0.0.0.0:8644</code>, webhook shutdown <code>5s</code>, web read-header <code>5s</code>, web shutdown <code>2s</code></td><td>Transport instance and lifecycle settings.</td></tr><tr><td>Telegram</td><td>typing refresh <code>4s</code>, draft interval <code>700ms</code>, update-action TTL <code>10m</code></td><td>Telegram adapter presentation and lifecycle settings.</td></tr><tr><td>Telegram limits</td><td>message length <code>4000</code> and callback/update prefixes</td><td>Record the upstream platform constraint and version it as an adapter compatibility limit; prefixes may remain wire constants when changing them would invalidate outstanding callbacks.</td></tr><tr><td>Containers</td><td>stop and pull timeouts <code>10s</code> and <code>30s</code></td><td>Container lifecycle policy.</td></tr><tr><td>HTTP clients</td><td>feasibility notification timeout <code>10s</code>; other paths use <code>http.DefaultClient</code></td><td>Adapter request policy. Every outbound client requires an explicit timeout profile.</td></tr><tr><td>Workflow routing</td><td>labels <code>bug</code>, <code>feature</code>, <code>bootstrap</code>; workflows <code>tdd</code>, <code>feasibility</code>, <code>bootstrap</code>; fallback <code>implement</code></td><td>Agent-system routing policy. Workflow identifiers are references, not compiled routing decisions.</td></tr><tr><td>Filesystem/deployment</td><td><code>.agents/skills</code>, <code>.archie/stages</code>, <code>.archie/gate.go</code>, <code>/data/worktree</code>, <code>/data/repo</code>, cache names/paths and prepared sentinel</td><td>Split into deployment settings, repository conventions, and genuine internal protocol markers. Paths an installation may relocate are settings.</td></tr><tr><td>Packaged release data</td><td><code>/usr/share/archie/CHANGELOG.archied.md</code> and <code>/usr/share/archie/CHANGELOG.archie.md</code></td><td>Packaging/deployment settings or paths derived from an installation prefix.</td></tr><tr><td>Forge presentation</td><td>default label colour <code>bfd4f2</code> and state-label colours</td><td>Forge adapter presentation settings.</td></tr><tr><td>Fallback system prompt</td><td>compiled fallback prompt</td><td>Agent-system default content, versioned and replaceable through its owning settings; safety constraints remain policy/code.</td></tr><tr><td>Default paths and identities</td><td>config path, data paths, listen addresses, model roles and workflow names</td><td>Typed defaults belonging to their respective owners, never global constants consumed throughout the application.</td></tr></tbody></table><p>This inventory is intentionally broader than exported Go <code>const</code> declarations. A literal hidden in a constructor, timeout, switch, retry loop, path join, prompt, label map, or client setup restricts behaviour just as effectively as an incorrectly used constant.</p><h2 id="constants-that-may-remain" tabindex="-1">Constants that may remain <a class="header-anchor" href="#constants-that-may-remain" aria-label="Permalink to &quot;Constants that may remain&quot;">​</a></h2><p>A value may remain constant only when its owner documents why runtime variation would be invalid. Typical valid cases are:</p><ul><li>domain enum values and state names;</li><li>a versioned wire-protocol version or framing token;</li><li>reserved internal headers and callback prefixes that peers must interpret identically;</li><li>compile-time mathematical relationships;</li><li>sentinel errors and internal type identifiers.</li></ul><p>Names such as a stream, consumer, label, workflow, file, directory, network, model role, timeout, retry count, capacity, size limit, colour, prompt, command, or executable are not invariants merely because the current application has only one of them.</p><p>Architecture tests SHOULD reject exported behavioural constants outside approved invariant packages and SHOULD detect direct consumption of private default constants outside their owning <code>DefaultSettings</code> construction.</p><h2 id="current-mutation-and-reload-ledger" tabindex="-1">Current mutation and reload ledger <a class="header-anchor" href="#current-mutation-and-reload-ledger" aria-label="Permalink to &quot;Current mutation and reload ledger&quot;">​</a></h2><p>The application currently changes behavioural values through unrelated paths:</p><table tabindex="0"><thead><tr><th>Current change seam</th><th>Current behaviour</th><th>Required target</th></tr></thead><tbody><tr><td>Telegram <code>/restart</code> in <code>cmd/archied</code></td><td>Re-reads the global document and directly replaces Telegram token and allowed-user IDs; failure retains old values.</td><td>Submit a Telegram-settings candidate through the universal change protocol. Validate the complete candidate, promote atomically, audit the acting identity, and retain last-known-good.</td></tr><tr><td>Active model selection</td><td><code>chatModelManager.SetActiveModel</code> changes a model reference.</td><td>A conversation/session command changing runtime state. Validate against configured model instances and audit it; do not rewrite configuration.</td></tr><tr><td>Active provider selection</td><td><code>chatModelManager.SetActiveProvider</code> changes provider selection.</td><td>A conversation/session command changing runtime state, with the same distinction from provider-instance configuration.</td></tr><tr><td>Persona selection</td><td><code>PersonaRegistry.SetActive</code> mutates an in-memory per-session choice.</td><td>Agent-system session state. Persistence and audit requirements are owned by that domain, not the configuration loader.</td></tr><tr><td>Memory provider <code>SaveConfig</code></td><td>Accepts an untyped map, delegates validation/persistence to an optional provider interface, then providers reload backing files.</td><td>A typed provider-owned configuration command using candidate validation, versioning, atomic promotion, health observation, audit, and rollback.</td></tr><tr><td>File and environment overlays</td><td>Applied while loading <code>internal/config</code>; defaults are filled by mutation in <code>finalize</code>.</td><td>Configuration-infrastructure input processing that produces separately typed candidates. Defaults and derivations remain visible in provenance.</td></tr></tbody></table><p>Any further mutation seam discovered during migration MUST be added here before it is changed. Direct field assignment on a live settings object is prohibited.</p><h2 id="universal-change-protocol" tabindex="-1">Universal change protocol <a class="header-anchor" href="#universal-change-protocol" aria-label="Permalink to &quot;Universal change protocol&quot;">​</a></h2><p>Changing configuration is an application command performed by an identity. It MUST:</p><ol><li>resolve the target owner and current version;</li><li>parse into that owner&#39;s typed settings;</li><li>validate syntax, semantic constraints, policy, references, and safe bounds;</li><li>stage a candidate without altering the active value;</li><li>run owner-defined readiness and health checks;</li><li>atomically promote the candidate and record actor, source, reason, diff, prior version, and new version;</li><li>observe the affected capability for a bounded period;</li><li>retain the new version on success, or automatically restore last-known-good on failure;</li><li>report the rejected or rolled-back candidate without terminating unaffected capabilities.</li></ol><p>Startup loading uses the same candidate rules. A bad user value therefore does not partially mutate the application or make a previously healthy capability unavailable.</p><p>Settings that cannot safely reload in-process are marked restart-required. The change may still be validated and versioned immediately, but promotion occurs through a controlled restart with the same health and rollback semantics.</p><h2 id="completion-criteria" tabindex="-1">Completion criteria <a class="header-anchor" href="#completion-criteria" aria-label="Permalink to &quot;Completion criteria&quot;">​</a></h2><p>The configuration rearchitecture is complete only when:</p><ul><li><code>internal/config</code> no longer exists;</li><li>every behavioural value has one classification and owner;</li><li>every current field and hard-coded value in the ledgers has a recorded disposition;</li><li>domains and plugins receive only their typed settings;</li><li>all changes use semantic commands and the universal change protocol;</li><li>active, candidate, rejected, rolled-back, and last-known-good versions are queryable with secret-safe provenance;</li><li>generated documentation lists every external key, default, constraint, mutability class, owner, and deprecation;</li><li>architecture tests prevent a new central runtime configuration model, direct live-setting mutation, and incorrectly fixed behavioural values.</li></ul>',57)])])}const m=t(n,[["render",d]]);export{h as __pageData,m as default};
