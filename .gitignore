/archied
/archie-agent
/bin/
.task/
*.db
.references/
.env
task.json

config.production.toml
.gotmp/

# Scratch
/tmp/
.crew/
.claude/
.codex/
.pi-subagents/
.pi/
.playwright-mcp/
# Working screenshots from UI sessions.
*.png

# Frontend
/ui/node_modules/
/ui/.vite/
/ui/*.tsbuildinfo
# ui/dist IS committed: the Go build must not need a Node toolchain.

# air live-reload
/.air/
/tmp/

# Beads / Dolt files (added by bd init)
.dolt/
.beads-credential-key
.beads/proxieddb/
*.gate.lock*
.impeccable/
.serena/
.beads/issues.jsonl
.beads/interactions.jsonl
.mcp.json

# gh CLI state (~/.local/state/gh) mistakenly created inside the tree
.local/
__pycache__/
*.pyc

# Local site build output.
/site/

# Locally-trusted development certificates (mkcert and friends). The private
# key is generated per machine and must never be committed.
*.pem
*.key
