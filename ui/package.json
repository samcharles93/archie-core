{
  "name": "archie-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --import ./test/register.js --test \"test/*.test.js\""
  },
  "dependencies": {
    "preact": "^10.26.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.0",
    "vite": "^8.0.0"
  }
}
