name: Deploy docs

on:
  push:
    branches: [main]
    paths:
      - "docs-2/**"
      - "tools/**"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # full history - VitePress lastUpdated needs it

      - uses: pnpm/action-setup@v6
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: pnpm
          cache-dependency-path: docs-2/pnpm-lock.yaml

      - uses: actions/cache@v4
        with:
          path: docs-2/.vitepress/cache
          key: ${{ runner.os }}-vitepress-${{ hashFiles('docs-2/**', 'docs-2/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-vitepress-

      - name: Install dependencies
        working-directory: docs-2
        run: pnpm install --frozen-lockfile

      - name: Build docs site
        working-directory: docs-2
        run: pnpm build

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs-2/.vitepress/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
